<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prestasi Siswa - SMPN SATU ATAP TUBAKI</title>
    <link rel="stylesheet" href="/css/style.css"> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    
    <style>
        /* Penyesuaian agar gaya halaman serasi */
        :root {
            --primary-blue: #003366;
            --accent-gold: #b8860b;
        }

        body {
            background: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        /* Hero Section Prestasi */
        .hero-section {
            background: linear-gradient(rgba(0, 51, 102, 0.8), rgba(0, 51, 102, 0.9)), url('/gambar/hero-bg.jpg');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 80px 20px;
            text-align: center;
        }

        .hero-section h1 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        /* Container Prestasi */
        .prestasi-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            padding: 50px 15px;
        }

        /* Card Style */
        .prestasi-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
            transition: 0.3s;
            display: flex;
            flex-direction: column;
            border: 1px solid #eee;
        }

        .prestasi-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
        }

        .prestasi-img-wrapper {
            width: 100%;
            height: 220px;
            background: #f0f0f0;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .prestasi-img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
            padding: 10px;
        }

        .prestasi-info {
            padding: 20px;
        }

        .badge-kategori {
            background: var(--accent-gold);
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.7rem;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .prestasi-info h3 {
            color: var(--primary-blue);
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .pemenang {
            font-size: 0.85rem;
            color: #666;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .empty-state {
            text-align: center;
            grid-column: 1 / -1;
            padding: 50px;
            color: #999;
        }

        /* Footer Back Button */
        .footer-action {
            text-align: center;
            margin-bottom: 50px;
        }

        .btn-back-home {
            background: var(--primary-blue);
            color: white;
            padding: 12px 25px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
            display: inline-block;
        }

        .btn-back-home:hover {
            background: #00509d;
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <div class="top-header">
        <div class="logo-wrapper">
            <img src="/gambar/logo1.png" alt="Logo" style="height: 50px; margin-right: 10px;">
            <div style="line-height: 1;">
                <span style="font-size: 0.7rem; color: #666;">Website Resmi</span><br>
                <span style="font-weight: bold; color: #003366;">SMPN SATU ATAP TUBAKI</span>
            </div>
        </div>
        <div class="date-info" id="currentDate"></div>
    </div>

    <nav>
        <div class="nav-container">
            <ul class="nav-menu">
                <li><a href="/">🏠 HOME</a></li>
                <li><a href="#">PROFIL ▼</a>
                    <ul class="submenu">
                        <li><a href="/guru">Guru dan Staf</a></li>
                        <li><a href="/tentang-sekolah">Tentang Sekolah</a></li>
                        <li><a href="/visimisi">Visi & Misi</a></li>
                    </ul>
                </li>
                <li><a href="#">AKADEMIK ▼</a>
                    <ul class="submenu">
                        <li><a href="/kurikulum">Kurikulum</a></li>
                        <li><a href="/kalender">Kalender Akademik</a></li>
                    </ul>
                </li>
                <li><a href="/prestasi" style="color: #ffcc00;">PRESTASI</a></li>
                
                <li><a href="#">KESISWAAN ▼</a>
                    <ul class="submenu">
                        <li><a href="/osis">OSIS</a></li>
                        <li><a href="/ekstrakurikuler">Ekstrakurikuler</a></li>
                    </ul>
                </li>
                <li><a href="/berita">BERITA</a></li>
                <li><a href="#">GALERI ▼</a>
                    <ul class="submenu">
                        <li><a href="/galeri">Foto Kegiatan</a></li>
                        <li><a href="/video">Video Dokumentasi</a></li>
                    </ul>
                </li>
                <li><a href="/kontak">KONTAK</a></li>
            </ul>
            <a href="/login" class="btn-login">👤 LOGIN</a>
        </div>
    </nav>

    <section class="hero-section">
        <h1>Prestasi Siswa</h1>
        <p>Inspirasi dari karya dan pencapaian siswa-siswi terbaik kami</p>
    </section>

    <div class="prestasi-container">
        <% if (typeof daftarPrestasi !== 'undefined' && daftarPrestasi.length > 0) { %>
            <% daftarPrestasi.forEach(pres => { %>
                <article class="prestasi-card">
                    <div class="prestasi-img-wrapper">
                        <img src="/gambar/prestasi/<%= pres.gambar %>" 
                             alt="<%= pres.judul %>" 
                             class="prestasi-img" 
                             onerror="this.src='/gambar/default-prestasi.jpg'">
                    </div>
                    
                    <div class="prestasi-info">
                        <span class="badge-kategori"><%= pres.kategori %></span>
                        <h3><%= pres.judul %></h3>
                        <div class="pemenang">
                            <i class="fas fa-award" style="color: #b8860b;"></i>
                            <span>Oleh: <strong><%= pres.pemenang %></strong></span>
                        </div>
                    </div>
                </article>
            <% }) %>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-trophy fa-4x" style="margin-bottom: 20px; opacity: 0.2;"></i>
                <h2>Belum Ada Data Prestasi</h2>
                <p>Nantikan update pencapaian terbaru dari siswa-siswi kami.</p>
            </div>
        <% } %>
    </div>

    <div class="footer-action">
        <a href="/" class="btn-back-home">
            <i class="fas fa-arrow-left"></i> Kembali ke Beranda
        </a>
    </div>

    <script>
        document.getElementById('currentDate').innerHTML = new Date().toLocaleDateString('id-ID', { 
            weekday: 'long', 
            year: 'numeric', 
            month: 'long', 
            day: 'numeric' 
        });
    </script>

</body>
</html>